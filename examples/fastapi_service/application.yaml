app:
  name: PythonForge Example
  env: development
  debug: true

server:
  http:
    host: "0.0.0.0"
    port: 8000
    cors_origins: []
    rate_limit_enabled: false
    # tls_enabled: true
    # cert_file: /path/to/cert.pem
    # key_file: /path/to/key.pem
    # ca_file: /path/to/ca.pem
    # mtls_required: false

logger:
  level: INFO
  format: json

trace:
  enabled: false
